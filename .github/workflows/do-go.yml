name: Go central workflow

on:
  push:
    branches: ['*']
    paths:
      - '**.go'
      - 'go.*'
      - 'etc/config.json'
      - '**/Makefile'
      - 'Dockerfile'
      - 'descriptors/ModuleDescriptor-template.json'
      - 'sonar-project.properties'
      - 'src/.errcheck-exclude'
      - '.golangci.yml'
      - '.github/workflows/do-go.yml'
    tags:
      - '[vV][0-9]+.[0-9]+.[0-9]+*'
  workflow_dispatch:

jobs:
  go:
    uses: folio-org/.github/.github/workflows/go.yml@FOLIO-3994-go-lint
    secrets: inherit
    with:
      docker-label-documentation: 'https://github.com/folio-org/ui-ldp/blob/master/doc/reports.md'
      lint-errcheck-excludes-file: 'src/.errcheck-exclude'
      lint-golangci-config-file: 'src/.golangci.yml'

